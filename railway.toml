[build]
builder = "DOCKERFILE"
dockerfilePath = "api-sonar-main/api-sonar-main/Dockerfile"

[deploy]
healthcheckPath = "/health/ready"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
